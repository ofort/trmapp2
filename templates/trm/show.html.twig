{% extends 'base.html.twig' %}

{% block title %}Trm{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-2"></div>
            <div class="col-8">
        <h1>Trm</h1>

        <table class="table">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ trm.id }}</td>
                </tr>
                <tr>
                    <th>Name</th>
                    <td>{{ trm.Name }}</td>
                </tr>
                <tr>
                    <th>Description</th>
                    <td>{{ trm.Description }}</td>
                </tr>
            <tr>
                <th>Versions</th>
                <td>
                    <table>
                        {% for version in trm.trmVersions %}
                            <tr>
                                <td><a href="{{ path('app_trm_version_show', {'id': version.id}) }}">{{ version.name }}</a></td>
                            </tr>
                        {% endfor %}
                    </table>

                </td>
            </tr>
            </tbody>
        </table>


        <a href="{{ path('app_trm_index') }}">Retour à la liste</a>

        <a href="{{ path('app_trm_edit', {'id': trm.id}) }}">Modifier</a>

        {{ include('trm/_delete_form.html.twig') }}
    </div>
        </div>
    </div>
{% endblock %}
